# predict-power — Python dependencies (CPU local dev + HPC training).
#
# For the live demo (HF Space) see space/requirements.txt instead — it pins
# slightly different versions for the gradio runtime.
#
# Training was performed under torch 2.3.1 + CUDA 11.8 + cuDNN 8.9 on
# NVIDIA A100 GPUs (see environment.yml for the matched conda spec).

torch==2.3.1
numpy>=1.26,<3
pandas>=2.0
matplotlib>=3.8
xarray>=2024.1
netcdf4>=1.6
scikit-learn>=1.3
tqdm>=4.66
