# predict-power — Python dependencies (CPU local dev + HPC training).
#
# For the live demo (HF Space) see space/requirements.txt instead — it pins
# slightly different versions for the gradio runtime.
#
# HPC training was performed under torch 2.3.1 + CUDA 11.8 + cuDNN 8.9
# inside the conda env at /cluster/tufts/c26sp1cs0137/pliu07/conda_envs/cs137.

torch==2.3.1
numpy>=1.26,<3
pandas>=2.0
matplotlib>=3.8
xarray>=2024.1
netcdf4>=1.6
scikit-learn>=1.3
tqdm>=4.66
